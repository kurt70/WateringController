# Scribe: commit and push note

Decision: Excluded local MCP config from commit.

Details:
- Created a commit containing UI refresh (layout and CSS updates) and squad/documentation updates.
- Deliberately excluded .copilot\mcp-config.json as it appears to be a local MCP configuration and not intended to be tracked.
